name: CI

on:
  push:
    branches: [dev, main]
  pull_request:
    branches: [dev, main]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          mkdir -p frontend/dist
          pip install -e ".[dev]"

      - name: Ruff check
        run: ruff check src/cachibot

      - name: Ruff format check
        run: ruff format --check src/cachibot

      - name: Bandit security scan
        run: bandit -r src/cachibot -c pyproject.toml

  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.10', '3.11', '3.12']
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          mkdir -p frontend/dist
          pip install -e ".[dev]"

      - name: Run tests
        run: pytest -v
